# Copy to .env and fill in values before running locally.
# In Docker the values are supplied via docker-compose environment: block.

DATABASE_URL=postgresql://uc:uc@localhost:5432/universal_captions
REDIS_URL=redis://localhost:6379

# Session TTL in Redis (seconds). Default 4 hours.
# SESSION_TTL=14400
